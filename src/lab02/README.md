# Лабораторная работа №2.Коллекция объектов.Вариант №5.


В работе использован класс `Bus` из первой лабораторной работы (автобус с номером маршрута, вместимостью, пассажирами и состоянием). 
Реализован класс-контейнер `BusFleet`, который управляет группой автобусов. 
Валидация вынесена в отдельный модуль `validation.py`

## Цель работы

Научиться работать с коллекциями объектов.
Понять разницу между моделью сущности и контейнером объектов.
Реализовать собственный контейнерный класс.
Освоить итерацию по объектам.
Реализовать базовые операции управления коллекцией.

## Краткое описание
В работе использован класс `Bus` из первой лабораторной работы (автобус с номером маршрута, вместимостью, пассажирами и состоянием). Реализован класс-контейнер `BusFleet`, который управляет группой автобусов. Валидация вынесена в отдельный модуль `validation.py`.


## Класс BusFleet (коллекция)

### Атрибуты

- `_items` – внутренний список объектов `Bus`.

### Основные методы

- `add(bus)` – добавляет автобус (проверяет тип и уникальность маршрута).
- `remove(bus)` – удаляет автобус по объекту.
- `remove_at(index)` – удаляет по индексу, возвращает удалённый объект.
- `get_all()` – возвращает копию списка.

### Поиск и фильтрация

- `find_by_route(route)` – возвращает первый автобус с указанным маршрутом.
- `find_all_by_status(status)` – возвращает новую коллекцию автобусов с заданным статусом.
- `find_all_with_passengers()` – возвращает коллекцию автобусов, в которых есть пассажиры.

### Сортировка

- `sort_by_route(reverse=False)`
- `sort_by_capacity(reverse=False)`
- `sort_by_passengers(reverse=False)`

### Магические методы

- `__len__` – поддержка `len()`.
- `__iter__` – поддержка `for`.
- `__getitem__` – поддержка индексации и срезов.
- `__str__`, `__repr__` – строковое представление.

## Сценарии

## Сценарий 1: Создание автопарка и добавление автобусов

Что происходит:

Создаются пять автобусов с разными маршрутами, вместимостью, количеством пассажиров и состояниями. 

Каждый автобус добавляется в коллекцию BusFleet. 

При успешном добавлении выводится сообщение Добавлен: <маршрут>.

Проверка дубликата: после добавления всех пяти автобусов предпринимается попытка добавить ещё один автобус с уже существующим маршрутом 101. 

Коллекция не допускает дублирования – выбрасывается исключение ValueError, которое перехватывается, и выводится сообщение: Ошибка: Автобус с маршрутом 101 уже существует.


<img width="1128" height="404" alt="image" src="https://github.com/user-attachments/assets/2740a92d-ceaf-4f05-8bcd-6f935189784b" />

 ## Сценарий 2: Вывод всех автобусов (итерация)
 
Что происходит:
Используется цикл for bus in fleet. 
Благодаря магическому методу `__iter__`и переопределённому `__str__` в классе Bus,
каждый автобус выводится в удобочитаемом виде: маршрут, пассажиры/вместимость, статус.

<img width="1192" height="320" alt="image" src="https://github.com/user-attachments/assets/e3eb7226-94c4-434f-aae6-bc7a92a1597a" />

## Сценарий 3: Поиск автобуса по маршруту
Что происходит:
Вызывается метод `find_by_route("103")`. 
Он возвращает первый автобус с указанным маршрутом. 
В данном случае найден автобус 103. 
Результат поиска выводится на экран.

<img width="1128" height="204" alt="image" src="https://github.com/user-attachments/assets/556d7ced-204e-425e-a313-5f5ac556b7d6" />

## Сценарий 4: Фильтрация коллекции (логические операции)
Что происходит:

`find_all_by_status("on_route")` – создаётся новая коллекция, содержащая только автобусы, 
находящиеся на маршруте (статус on_route). 
Выводятся все такие автобусы (101, 103, 105).

`find_all_with_passengers()` – создаётся новая коллекция из автобусов, в которых есть пассажиры (passengers > 0).
Результат: те же автобусы 101, 103, 105 (у них пассажиры есть, а у 102 и 104 – ноль).

Обе фильтрации не изменяют исходную коллекцию, а возвращают новые объекты BusFleet.

<img width="1230" height="418" alt="image" src="https://github.com/user-attachments/assets/d705072b-a35f-463b-a51c-ac19406140bc" />


## Сценарий 5: Сортировка коллекции
Что происходит:

`sort_by_capacity()` – сортирует автобусы по вместимости (по возрастанию). 
Вывод: сначала маршрут 104 (30 мест), затем 103 (40), 101 (50), 105 (55), 102 (60).

`sort_by_route(reverse=True)` – сортирует по номеру маршрута в обратном порядке (убывание). 
Вывод: 105, 104, 103, 102, 101.
Сортировка изменяет порядок элементов в самой коллекции

<img width="1266" height="526" alt="image" src="https://github.com/user-attachments/assets/33cc3c71-2928-49be-b97e-e9261f69e066" />


## Сценарий 6: Индексация и срезы (магический метод __getitem__)
Что происходит:

`fleet[0]` – доступ к первому элементу коллекции (после сортировки первым идёт 105). 
Выводится его строковое представление.

`fleet[2]` – третий элемент (индекс 2) – автобус 103.

`fleet[1:3]` – срез, возвращающий список автобусов с индексами 1 и 2 (104 и 103). 
Выводятся их __repr__-представления.

<img width="1252" height="240" alt="image" src="https://github.com/user-attachments/assets/e8535a13-9200-49bc-b50c-198f44b4b50c" />


## Сценарий 7: Удаление элементов
Что происходит:

Удаление по объекту: `fleet.remove(b2)` – удаляется автобус с маршрутом 102.
После удаления в коллекции остаётся 4 автобуса. Выводится их список.


Удаление по индексу:` fleet.remove_at(2)` – удаляется элемент с индексом 2 (в текущем порядке это автобус 103). 
Удалённый автобус возвращается методом и выводится. Остаётся 3 автобуса.


<img width="1228" height="580" alt="image" src="https://github.com/user-attachments/assets/dbf9f760-33d7-48ab-bd6f-16c41a539e2c" />

## Сценарий 8: Использование len() и повторная итерация
Что происходит:

`len(fleet)` возвращает текущее количество автобусов (3).

Цикл `for i, bus in enumerate(fleet)` выводит индекс и номер маршрута каждого оставшегося автобуса.

<img width="1212" height="318" alt="image" src="https://github.com/user-attachments/assets/07ab2a3c-a60f-4126-af9c-015309d89cd9" />

## Сценарий 9: Изменение состояния автобусов (бизнес-логика)
Что происходит:

Из текущей коллекции (после удалений) отбираются автобусы со статусом parked. Это автобус 104 (у него статус parked).

Для каждого такого автобуса вызывается метод `start_route()`, который переводит его в статус on_route.

Выводится сообщение Автобус 104 выехал на маршрут.

Демонстрируется, что коллекция хранит ссылки на объекты, и их состояние может меняться через бизнес-методы.

<img width="1078" height="154" alt="image" src="https://github.com/user-attachments/assets/5e289565-4d60-4594-838e-8fbe57926055" />

## Сценарий 10: Обработка ошибок – добавление некорректного типа

Что происходит:

Пытаемся добавить в коллекцию строку "это строка, не автобус" вместо объекта Bus.

Метод `add()` проверяет тип через isinstance и выбрасывает TypeError.

Исключение перехватывается, выводится сообщение: Ошибка: Можно добавлять только объекты Bus.

Программа не падает, продолжает выполнение.

<img width="1194" height="132" alt="image" src="https://github.com/user-attachments/assets/50f43ba7-c849-40aa-9885-4740bcfea89c" />







